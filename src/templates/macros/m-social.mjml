{% macro social(name, url, size, src) %}
	<mj-social-element
		padding="0 15px" 
		name="{{ name }}" 
		href="{{ url }}"
		icon-size="{{ size }}"
		src="{{ src }}" 
		background-color="none">
	</mj-social-element>
{% endmacro %}